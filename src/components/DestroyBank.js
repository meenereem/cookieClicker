import React from 'react'

const DestroyMine = (props) => (
    <div>
    <a className="btn btn-primary"  href="#" onClick={props.destroyBank}>Demolish Bank</a>
    </div>

)

export default DestroyMine;